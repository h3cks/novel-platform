/** @type {import('next').NextConfig} */
const nextConfig = {
  /* налаштування тут */
  reactStrictMode: true,
  // Якщо у вас були специфічні налаштування, додайте їх сюди
};

export default nextConfig;